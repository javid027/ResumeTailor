name: resume_tailor
description: "ResumeTailor — AI-tailored resumes with diff review."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_bloc: ^9.1.1
  equatable: ^3.0.0
  freezed_annotation: ^3.1.0
  json_annotation: ^4.12.0
  get_it: ^9.2.1
  injectable: ^3.0.0
  dio: ^5.11.1
  go_router: ^14.8.1
  file_picker: ^13.1.0
  share_plus: ^13.3.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  in_app_purchase: ^3.3.0
  flutter_secure_storage: ^11.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  build_runner: ^2.15.1
  freezed: ^3.2.6-dev.1
  json_serializable: ^6.14.1
  injectable_generator: ^3.1.3
  bloc_test: ^10.0.0
  mocktail: ^1.0.5

flutter:
  uses-material-design: true
